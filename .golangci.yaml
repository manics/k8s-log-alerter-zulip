# https://golangci-lint.run/docs/configuration/file/
version: "2"

linters:
  default: standard
  enable:
    - gocritic
    - gosec
    - govet
    - modernize
    - staticcheck

formatters:
  enable:
    # - gci
    - gofmt
    - gofumpt
    # - goimports
    - golines
    - swaggo
