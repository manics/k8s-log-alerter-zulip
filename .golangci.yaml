# https://golangci-lint.run/docs/configuration/file/
version: "2"

linters:
  default: standard
  enable:
    - contextcheck
    - gocritic
    - gosec

formatters:
  enable:
    - gci
    - gofmt
    - gofumpt
    - goimports
    - golines
    - swaggo